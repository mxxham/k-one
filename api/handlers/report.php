<?php
require_once __DIR__ . '/reports.php';

if (!function_exists('handle_report')) {
    function handle_report($action) { return handle_report($action); }
}
if (!function_exists('handle_activitylog')) {
    function handle_activitylog($action) { return handle_activitylog($action); }
}